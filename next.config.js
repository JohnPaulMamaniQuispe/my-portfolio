/** @type {import('next').NextConfig} */
const nextConfig = {
    // Configuración básica para el portafolio
    images: {
      domains: ['localhost'], // Para imágenes locales
    },
  };
  
  module.exports = nextConfig;